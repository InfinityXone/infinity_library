SAFE SECRET SCAN TOOL
======================

This folder contains:

- safe_secret_scan.sh
- secret_inventory.txt (when generated)

To run the scan:

    ./safe_secret_scan.sh

To open the results later:

    nano secret_inventory.txt

You can also open and edit the script itself:

    nano safe_secret_scan.sh
